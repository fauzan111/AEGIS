.venv/
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# local Claude Code state and installed-skill tooling - not project source
.claude/
.agents/
skills-lock.json

# /brag skill scratch output (render thumbnails, composition build files) -
# the actual finished deliverable is copied to slides/GATE-2/AEGIS.mp4
brag-output/

# Office lock files
~$*

# OS cruft
.DS_Store
Thumbs.db
.gstack/

# local backup copies / scratch dumps, not meant for the public repo
slides/GATE-2/BACKUP FILES/
*.BACKUP*.docx
*.BACKUP*.pptx
gate2_dump*.txt

# raw source video/poster for the Gate 2 slide 13 demo clip - already
# embedded inside AEGIS_Gate2_Official.pptx, keeping the loose 58MB .mp4
# out of git keeps the repo from bloating with a duplicate copy
slides/GATE-2/AEGIS_Live_Demo.mp4
slides/GATE-2/AEGIS_Live_Demo_poster.png
